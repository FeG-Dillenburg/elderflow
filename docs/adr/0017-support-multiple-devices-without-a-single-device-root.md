# Support multiple devices without a single-device root

The Protected-text key architecture must let a Content user unlock independently on multiple browsers and devices, including smartphones, without making one previously enrolled device the sole root of access. Device-specific keys may be evaluated as an additional mechanism, but access and recovery cannot depend on possession of a single device, and the design remains independent from local login passwords.
