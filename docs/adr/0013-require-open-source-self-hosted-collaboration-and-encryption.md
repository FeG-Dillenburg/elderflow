# Require open-source self-hosted collaboration and encryption

ElderFlow's collaborative editing and end-to-end encryption architecture must use open-source, self-hostable components for the editor, synchronization protocol and server, persistence format, and cryptographic implementation. Proprietary collaboration services and paid-only editor extensions are excluded to keep installations independently operable and avoid vendor lock-in, even when a hosted product would reduce implementation effort.
